@model List<EntityLayer.Concrete.Coach>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Teknik Adam PAGE</h1>

<div class="container">
    <div class="column">
        <div class="col-12">
            <h3 class="titulo-tabla">Teknikler </h3>
            <table id="ejemplo" class="table table-striped table-bordered" style="width:100%">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Sil</th>
                        <th>Güncelle</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Name</td>
                            <td><a href="/Admin/Coach/UpdateCoach/@item.CoachID" class="btn btn-outline-success">Düzenle</a></td>
                            <td><a href="/Admin/Coach/DeleteCoach/@item.CoachID" class="btn btn-outline-danger">Sil</a></td>
                        </tr>
                    }
                </tbody>
            </table>
            <a href="/Admin/Coach/AddCoach" class="btn btn-outline-light">Yeni Teknik Girişi</a>
        </div>
    </div>
</div>
